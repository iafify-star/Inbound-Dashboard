# ملاحظات التيم — شغل يدوي

الملاحظات الأساسية جوّه الكود نفسه. ابدأ من الملفات دي:

1. `js/inbound-config.js` — السويتشات. متعملش فيها حاجة قبل العرض.
2. `dashboard.html` — فوق كل بلوك مكتوب إيه يتعدل: شيت، ترجمة، مستودع، عمود، شارت، جدول.
3. `js/inbound-client.js` — مصدر الداتا.
4. `firebase/firestore.rules` — قفل الموظفين (لسه متعملش deploy).

العرض المحلي: http://127.0.0.1:5500/dashboard.html  
الربط لاحقاً: `FIREBASE_PRODUCTION.md`
